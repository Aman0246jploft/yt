<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background: white;
            border-radius: 15px;
            margin-bottom: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .header h1 {
            color: #333;
            font-size: 1.8em;
            max-width: 70%;
        }
        
        .video-info {
            background: white;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            display: flex;
            gap: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .video-thumbnail {
            flex-shrink: 0;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 8px 25px rgba(0,0,0,0.2);
        }
        
        .video-thumbnail img {
            width: 320px;
            height: 180px;
            object-fit: cover;
        }
        
        .video-details {
            flex: 1;
        }
        
        .video-details h2 {
            color: #333;
            margin-bottom: 15px;
            font-size: 1.8em;
        }
        
        .video-meta {
            display: flex;
            gap: 20px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }
        
        .meta-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #666;
            font-size: 0.95em;
        }
        
        .meta-item strong {
            color: #667eea;
        }
        
        .formats-container {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .formats-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }
        
        .formats-header h3 {
            color: #333;
            font-size: 1.5em;
        }
        
        .formats-count {
            background: #667eea;
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 600;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        th {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            text-align: left;
            font-weight: 600;
            font-size: 1.1em;
        }
        
        td {
            padding: 18px 20px;
            border-bottom: 1px solid #eef2ff;
        }
        
        tr:hover {
            background: #f8f9ff;
            transform: scale(1.01);
            transition: transform 0.2s;
        }
        
        .quality-badge {
            background: #667eea;
            color: white;
            padding: 6px 12px;
            border-radius: 6px;
            font-weight: 600;
            font-size: 0.9em;
        }
        
        .format-type {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 6px;
            font-size: 0.9em;
            font-weight: 500;
        }
        
        .type-video-audio {
            background: #e8f5e9;
            color: #2e7d32;
        }
        
        .type-video-only {
            background: #e3f2fd;
            color: #1565c0;
        }
        
        .type-audio-only {
            background: #f3e5f5;
            color: #7b1fa2;
        }
        
        .codec-badge {
            display: inline-block;
            padding: 4px 8px;
            background: #e0e6ff;
            border-radius: 4px;
            font-size: 0.8em;
            color: #667eea;
            margin-right: 5px;
            font-family: monospace;
        }
        
        .btn {
            padding: 12px 24px;
            background: linear-gradient(135deg, #56ab2f 0%, #a8e063 100%);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s;
            box-shadow: 0 4px 15px rgba(86, 171, 47, 0.4);
        }
        
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(86, 171, 47, 0.6);
        }
        
        .btn-secondary {
            background: linear-gradient(135deg, #6c757d 0%, #495057 100%);
            box-shadow: 0 4px 15px rgba(108, 117, 125, 0.4);
        }
        
        .btn-small {
            padding: 8px 16px;
            font-size: 14px;
        }
        
        .actions {
            display: flex;
            gap: 15px;
            margin-top: 30px;
            justify-content: center;
        }
        
        .quick-downloads {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            padding: 25px;
            border-radius: 15px;
            margin-top: 30px;
            text-align: center;
        }
        
        .quick-downloads h4 {
            margin-bottom: 20px;
            color: #333;
        }
        
        .quick-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
        }
        
        .audio-only-section {
            background: #f3e5f5;
            padding: 20px;
            border-radius: 10px;
            margin-top: 30px;
        }
        
        .audio-only-section h4 {
            color: #7b1fa2;
            margin-bottom: 15px;
        }
        
        .footer {
            text-align: center;
            margin-top: 40px;
            padding: 20px;
            color: white;
            opacity: 0.8;
        }
        
        @media (max-width: 768px) {
            .video-info {
                flex-direction: column;
            }
            
            .video-thumbnail {
                align-self: center;
            }
            
            .video-thumbnail img {
                width: 100%;
                height: auto;
            }
            
            .header {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }
            
            .header h1 {
                max-width: 100%;
                font-size: 1.5em;
            }
            
            table {
                display: block;
                overflow-x: auto;
            }
            
            .quick-buttons {
                flex-direction: column;
            }
            
            th, td {
                padding: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Download Options for: <%= videoInfo.title %></h1>
            <a href="/" class="btn btn-secondary">
                <span>←</span> Back to Home
            </a>
        </div>
        
        <div class="video-info">
            <div class="video-thumbnail">
                <img src="<%= videoInfo.thumbnail %>" alt="Video Thumbnail">
            </div>
            <div class="video-details">
                <h2><%= videoInfo.title %></h2>
                <div class="video-meta">
                    <div class="meta-item">
                        <span>👤</span>
                        <strong>Author:</strong> <%= videoInfo.author %>
                    </div>
                    <div class="meta-item">
                        <span>⏱️</span>
                        <strong>Duration:</strong> <%= videoInfo.durationFormatted %>
                    </div>
                    <div class="meta-item">
                        <span>🎬</span>
                        <strong>Formats:</strong> <%= videoInfo.formats.length %> available
                    </div>
                </div>
                <p><strong>Video ID:</strong> <%= videoInfo.videoId %></p>
            </div>
        </div>
        
        <div class="formats-container">
            <div class="formats-header">
                <h3>Available Formats</h3>
                <div class="formats-count"><%= videoInfo.formats.length %> Formats</div>
            </div>
            
            <% if (videoInfo.formats.length > 0) { %>
                <table>
                    <thead>
                        <tr>
                            <th>Quality</th>
                            <th>Format & Codecs</th>
                            <th>Size</th>
                            <th>Type</th>
                            <th>Download</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% videoInfo.formats.forEach(format => { 
                            const typeClass = format.hasVideo && format.hasAudio ? 'type-video-audio' :
                                            format.hasVideo && !format.hasAudio ? 'type-video-only' :
                                            'type-audio-only';
                            
                            const typeText = format.hasVideo && format.hasAudio ? 'Video + Audio' :
                                           format.hasVideo && !format.hasAudio ? 'Video Only' :
                                           'Audio Only';
                            
                            const typeIcon = format.hasVideo && format.hasAudio ? '🎬' :
                                           format.hasVideo && !format.hasAudio ? '🎞️' :
                                           '🎵';
                            
                            const downloadFilename = sanitizeFilename(videoInfo.title) + '.' + format.container;
                            const downloadUrl = `/api/video/download?url=${encodeURIComponent(videoUrl)}&quality=${format.itag}&filename=${encodeURIComponent(downloadFilename)}`;
                        %>
                        <tr>
                            <td>
                                <span class="quality-badge"><%= format.quality %></span>
                            </td>
                            <td>
                                <div style="margin-bottom: 8px;">
                                    <strong><%= format.container.toUpperCase() %></strong>
                                </div>
                                <% if (format.videoCodec) { %>
                                    <span class="codec-badge"><%= format.videoCodec.split('.')[0] %></span>
                                <% } %>
                                <% if (format.audioCodec) { %>
                                    <span class="codec-badge"><%= format.audioCodec.split('.')[0] %></span>
                                <% } %>
                                <div style="margin-top: 8px; font-size: 0.9em; color: #666;">
                                    <%= format.fps ? format.fps + 'fps' : '' %>
                                    <%= format.bitrate ? '• ' + format.bitrate + 'kbps' : '' %>
                                </div>
                            </td>
                            <td>
                                <strong><%= format.filesizeFormatted %></strong>
                            </td>
                            <td>
                                <span class="format-type <%= typeClass %>">
                                    <%= typeIcon %> <%= typeText %>
                                </span>
                            </td>
                            <td>
                                <a href="<%= downloadUrl %>" 
                                   class="btn btn-small" 
                                   download="<%= downloadFilename %>">
                                    <span>⬇️</span> Download
                                </a>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <div style="text-align: center; padding: 40px; color: #666;">
                    <p style="font-size: 1.2em;">No formats available for this video.</p>
                </div>
            <% } %>
            
            <div class="quick-downloads">
                <h4>Quick Downloads</h4>
                <div class="quick-buttons">
                    <a href="/api/video/simple-download?url=<%= encodeURIComponent(videoUrl) %>&quality=highest" 
                       class="btn" target="_blank">
                        <span>⬇️</span> Download Highest Quality
                    </a>
                    <a href="/api/video/simple-download?url=<%= encodeURIComponent(videoUrl) %>&quality=lowest" 
                       class="btn" target="_blank">
                        <span>⬇️</span> Download Lowest Quality
                    </a>
                </div>
            </div>
            
            <% 
                const audioFormats = videoInfo.formats.filter(f => !f.hasVideo && f.hasAudio);
                if (audioFormats.length > 0) { 
            %>
                <div class="audio-only-section">
                    <h4>🎵 Audio Only Formats</h4>
                    <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 15px; margin-top: 15px;">
                        <% audioFormats.forEach(format => { 
                            const downloadFilename = sanitizeFilename(videoInfo.title) + '_audio.' + format.container;
                            const downloadUrl = `/api/video/download?url=${encodeURIComponent(videoUrl)}&quality=${format.itag}&filename=${encodeURIComponent(downloadFilename)}`;
                        %>
                            <div style="background: white; padding: 15px; border-radius: 8px; display: flex; justify-content: space-between; align-items: center;">
                                <div>
                                    <strong><%= format.container.toUpperCase() %></strong>
                                    <div style="font-size: 0.9em; color: #666;">
                                        <%= format.bitrate ? format.bitrate + 'kbps' : '' %>
                                        • <%= format.filesizeFormatted %>
                                    </div>
                                </div>
                                <a href="<%= downloadUrl %>" 
                                   class="btn btn-small" 
                                   download="<%= downloadFilename %>">
                                    <span>⬇️</span>
                                </a>
                            </div>
                        <% }) %>
                    </div>
                </div>
            <% } %>
            
            <div class="actions">
                <a href="/" class="btn btn-secondary">
                    <span>←</span> Back to Home
                </a>
                <a href="/process" class="btn" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                    <span>🔄</span> Process Another Video
                </a>
            </div>
        </div>
        
        <div class="footer">
            <p>© 2024 YouTube Downloader • Videos are property of their respective owners</p>
            <p style="font-size: 0.9em; margin-top: 5px;">Use this tool responsibly and respect copyright laws.</p>
        </div>
    </div>
</body>
</html>